{
  "name": "redis-scheduled",
  "version": "0.3.1",
  "description": "a scheduled module use redis",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "redis",
    "schedule",
    "scheduled",
    "redis-schedule",
    "redis-scheduled"
  ],
  "author": "xuezi",
  "license": "ISC",
  "dependencies": {
    "redis": "^2.6.3"
  }
}
